<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      Face enrolment / check-in selfie.

      The web app calls navigator.mediaDevices.getUserMedia(). Inside a
      Capacitor WebView that reaches Android's permission layer, and with
      no CAMERA permission declared the request is refused outright - which
      surfaces in the UI as "Camera permission was denied - allow camera
      access for this site and try again". Declaring it here lets
      Capacitor's BridgeWebChromeClient raise the runtime prompt the first
      time a face capture is attempted.
    -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!--
      Geofenced attendance.

      navigator.geolocation backs both "Use my current location" in Add
      Office Location and the radius check at check-in. Without these the
      WebView resolves nothing and the UI reports "Could not get current
      location". FINE is what a 150m office radius needs; COARSE is the
      fallback when a user grants only approximate location on Android 12+.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!--
      required="false" so Play Store still lists the app for devices with
      no camera - every other module works without one.
    -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
</manifest>
